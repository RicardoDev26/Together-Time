import React, {useState} from "react";
import { StyledView, StyledText, StyledTextInput, StyledTouchableOpacity, StyledImage } from "../../shared/styled";
import {Select, SelectSection, SelectItem} from "@nextui-org/select";
import { TypeEvent } from "../../types/types";





const InputTypeEvent: React.FC = () => {
    return(
        <StyledView className="p-5">        
        </StyledView>
    )
}

export default InputTypeEvent